# 디자인 FTO 챗봇 - 유사 이미지 검색 개선 작업

## 프로젝트 배경
- 화장품 용기(로카르노 분류 9-1) 디자인 특허 FTO 보조 챗봇
- 쿼리 이미지 → ChromaDB Top 10 검색 → 사용자 선택 → VLM 분석 → FTO 리포트 생성
- 현재 스택: CLIP 임베딩 + 코사인 유사도 + ChromaDB
- 이미지 전처리: OpenCV 스케치 변환 (GaussianBlur 5×5 σ=1.0 → Canny 30/120 → Dilate 2×2 1회)
- 전체 DB: 약 20,000장 (KIPRIS 수집)

## 개선 목표
코사인 유사도만으로는 검색 품질이 불안정함.
→ **이미지 + 텍스트 하이브리드 검색 + Percentile ranking** 도입.

---

## Task 1: Gemini로 형태 특징 추출 (DB 라벨링)

**모델**: Gemini 1.5 Flash

**Gemini 프롬프트**:
아래 화장품 용기 디자인 특허 이미지를 특허 심사관 관점에서 분석해줘.
다음 항목 중심으로 형태 특징을 추출하고, 아래 JSON 형식으로만 반환해. 다른 텍스트 없이.

{
  "실루엣": "",
  "연결부형태": "",
  "세부조형": [],
  "디스펜서": "",
  "뚜껑": "",
  "주요특징요약": ""
}

**실행 방식**:
- 먼저 100장 테스트 → 결과 확인 → 전체 20,000장 실행
- 요청 간 1초 딜레이 (rate limit 방지)
- 실패한 이미지는 failed_list.txt에 기록
- 결과 저장: labels/{patent_id}.json

---

## Task 2: 쿼리 이미지 형태 특징 추출 함수

- Task 1과 완전히 동일한 프롬프트 사용 (일관성 유지)
- 사용자 쿼리 이미지 입력 시 실시간 추출
- 결과는 텍스트 임베딩 생성에 사용

---

## Task 3: 이미지 + 텍스트 하이브리드 검색
```
쿼리 이미지
  ├─▶ 스케치 변환 → CLIP 임베딩 → 이미지 유사도 점수
  └─▶ Gemini 형태 특징 추출 → 텍스트 임베딩 → 텍스트 유사도 점수

최종 점수 = 이미지 유사도 × α + 텍스트 유사도 × (1 - α)
```

- 기본값: α = 0.7
- 0.1 단위로 실험하여 최적값 탐색
- 텍스트 임베딩: paraphrase-multilingual-MiniLM-L12-v2 또는 text-embedding-3-small
- 기존 ChromaDB 컬렉션 유지, 텍스트 컬렉션 별도 추가

---

## Task 4: Percentile Ranking 점수 정규화
```python
# "0.87점" 대신 "상위 3%"로 표현
score_percentile = (DB 내 해당 점수보다 낮은 항목 수 / 전체 DB 수) * 100
```

- DB 전체 점수 분포 미리 샘플링하여 기준값 저장
- UI에는 percentile로 표시 (예: "상위 5%")

---

## 주의사항
- Gemini API 키는 환경변수로 관리 (GEMINI_API_KEY)
- Task 1 라벨링 결과 반드시 백업
- 각 Task는 독립적으로 실행 가능하게 모듈화
- 기존 ChromaDB 컬렉션 건드리지 말고 새 컬렉션/필드 추가 방식으로