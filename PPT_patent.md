# FTOGuard - 특허 FTO 분석 파이프라인

## 전체 흐름 요약

```
사용자 제품 설명 입력
    │
    ▼
┌─────────────────────────────────────┐
│  1. Pre-Filter (키워드 사전필터링)     │  78,000 → ~1,000 로 축소
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  2. Hybrid Search (Dense + Sparse)   │  축소된 풀 내에서 정밀 검색
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  3. RRF + Patent Collapse            │  순위 합산 → 특허 단위 통합
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  4. RDB Filter + 데이터 보강          │  등록 상태 확인 + 메타데이터 통합
└─────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────┐
│  5. sLLM Generation (Qwen2.5-14B)   │  침해 여부 분석 리포트 생성
└─────────────────────────────────────┘
    │
    ▼
  FTO 분석 결과 반환
```

---

## 1단계: Pre-Filter (키워드 사전필터링)

### 목적
- 전체 78,000개 청크를 대상으로 Dense/Sparse 검색을 수행하면 비용과 시간이 과다
- **키워드 매칭**으로 관련 없는 문서를 사전에 제거하여 검색 풀을 ~1,000개로 축소

### 처리 과정

```
사용자 입력: "헤스페리딘이 포함된 미백 화장품"
    │
    ▼
[키워드 추출] (filter.py → extract_keywords)
    - 청구항 접두사 제거 ("제1항에 있어서" 등)
    - 특수문자 제거 (괄호, 마침표 등)
    - 조사/어미 단계적 제거 ("포함하는" → "포함" → 불용어 제거)
    - 불용어 필터 (160+ 단어: "상기", "본", "발명" 등)
    - 노이즈 패턴 필터 (숫자+단위, "제N항" 등)
    │
    결과: ["헤스페리딘", "미백", "화장품"]
    │
    ▼
[DB 조회] (claim_keywords 테이블)
    - 1,000만건의 (patent_id, chunk_id, keyword) 인덱스
    - 키워드 IN 매칭 → chunk_id별 매칭 횟수(cnt) 기준 내림차순 정렬
    - 상위 1,000개 청크 반환
    │
    결과: (patent_ids, chunk_ids) → 리트리버에 전달
```

### 데이터 소스
| 소스 | 위치 | 규모 |
|------|------|------|
| claim_keywords (MySQL/SQLite) | RDS 또는 로컬 SQLite | ~1,000만건 |
| 키워드 사전 (config.py) | 조사/어미/불용어/노이즈 패턴 | 160+ 불용어, 25+ 노이즈 패턴 |

### 핵심 설계
- **MySQL 우선, SQLite 폴백**: 환경에 따라 자동 선택 (최초 1회 감지)
- **키워드 정규화**: 인덱싱 시와 검색 시 동일한 전처리 적용 (tokenizer.py = filter.py)
- **매칭 없으면 전체 검색 fallback**: 사전필터링 결과가 없으면 78K 전체 대상 검색

---

## 2단계: Hybrid Search (Dense + Sparse)

사전필터링으로 축소된 ~1,000개 청크 풀 내에서 두 가지 검색을 **병렬** 수행

### Dense Search (의미 검색)

```
사용자 쿼리
    │
    ▼
[KURE-v1 임베딩] (nlpai-lab/KURE-v1, 1024차원)
    - 한국어 특화 문장 임베딩 모델
    - 쿼리를 1024차원 벡터로 변환
    │
    ▼
[ChromaDB 검색] (cosine distance)
    - HNSW 인덱스 기반 ANN(Approximate Nearest Neighbor) 검색
    - 사전필터링된 patent_id 목록으로 where 필터 적용
    - Top 50개 반환 (distance 낮을수록 유사)
```

- **임베딩 모델**: KURE-v1 (한국어 특화 문장 임베딩 모델)
- **벡터 DB**: ChromaDB (cosine distance, HNSW 인덱스)
- **검색 범위**: 사전필터링된 patent_id의 청크만 검색 (where 필터)

### Sparse Search (키워드 검색)

```
사용자 쿼리
    │
    ▼
[형태소 토크나이징] (tokenizer.py → morpheme_tokenize)
    - regex 기반 도메인 특화 키워드 추출
    - 조사/어미 제거, 불용어 필터
    │
    ▼
[BM25 스코어링] (역색인 inverted index)
    - postings.pkl: 단어 → [(doc_id, tf), ...] 역색인
    - idf.pkl: 단어별 IDF 값
    - doc_len.pkl: 문서별 길이
    - BM25 Okapi 공식: idf × tf×(k1+1) / (tf + k1×(1-b+b×dl/avgdl))
    - 사전필터링된 chunk_id → doc_id로 변환하여 후보 제한
    - Top 50개 반환 (score 높을수록 관련)
```

- **토크나이저**: regex 기반 도메인 특화 규칙 (조사/어미/불용어 사전)
- **BM25 파라미터**: k1=1.5, b=0.75
- **분할 청크 처리**: 긴 청구항은 _sub, _win 접미사로 분할 → 역매핑으로 누락 방지

### Hybrid Search가 필요한 이유
| | Dense (의미) | Sparse (키워드) |
|---|---|---|
| 강점 | 동의어/유사 표현 매칭 | 정확한 용어 매칭, 희귀 단어 |
| 약점 | 도메인 전문 용어 놓침 | 의미적 유사성 파악 불가 |
| 예시 | "미백" ≈ "화이트닝" 매칭 | "헤스페리딘" 정확히 매칭 |

---

## 3단계: RRF (Reciprocal Rank Fusion) + Patent Collapse

### RRF 합산

```
Dense Top 50 + Sparse Top 50
    │
    ▼
[RRF 공식]
    score(d) = w_dense/(k + rank_dense) + w_sparse/(k + rank_sparse)
    │
    - k = 60 (순위 편향 완화 파라미터)
    - w_dense = 0.2, w_sparse = 0.8 (rrf_sweep 실험 최적값)
    - 한쪽에만 있는 청크는 최하위 순위 부여 (페널티)
    │
    ▼
  합산 결과 (모든 고유 chunk_id의 RRF 점수)
```

- **가중치 (0.2:0.8)**: Sparse(BM25) 비중이 높음 → 특허 도메인에서 정확한 용어 매칭이 중요
- **rrf_sweep으로 최적화**: 실험을 통해 도출된 최적 가중치

### Patent Collapse (특허 단위 통합)

```
RRF 합산 결과 (청크 단위)
    │
    ▼
[Patent Collapse]
    - chunk_id에서 출원번호 추출 (형식: {apply_num}_claim_{num})
    - 같은 특허의 여러 청크 중 최고 RRF 점수 1개만 유지
    - 매칭된 청구항 번호 기록 (matched_claim_num)
    │
    ▼
  Top 10 특허 (점수 내림차순)
```

- **목적**: 하나의 특허가 여러 청크(청구항)로 검색될 수 있음 → 대표 1개로 축소
- **최종 결과**: 상위 10건의 특허 (FINAL_TOP_K = 10)

---

## 4단계: RDB Filter + 데이터 보강

### 등록 상태 필터링

```
Top 10 특허
    │
    ▼
[ParentDB 조회] (parents.sqlite 또는 MySQL RDS)
    - 78,520건의 특허 메타데이터 + 청구항 원문 통합 DB
    - 허용 상태: "등록", "공개"만 통과
    - 소멸/거절/취하 등 → 제거
```

### 데이터 보강

```
필터 통과 특허
    │
    ▼
[메타데이터 보강]
    - 발명 명칭, 초록, 등록번호, 출원일, 등록일, 출원인, IPC 분류
    - 등록 청구항 원문 (claim_regit)
    - 공개 청구항 원문 (claim_pub) → 금반언 분석용
    │
    ▼
[구성요소 조회] (claim_components 테이블, 26만건)
    - patent_id 배치 조회 (N+1 쿼리 방지)
    - 각 청구항의 구성요소 목록 + 참조 종속항 번호
    │
    ▼
[MIN_SCORE 필터]
    - RRF 점수 0.010 미만 → 노이즈로 제거
    │
    ▼
  최종 검색 결과 (보강된 특허 데이터)
```

### 보강 후 데이터 구조
```python
{
    "patent_id": "1020210065562",
    "score": 0.025,
    "matched_claim_num": 1,
    "metadata": {
        "invention_title": "...",
        "applicant": "...",
        "register_status": "등록",
        "ipc": ["A61K 8/00", ...],
    },
    "claims": {
        "claim_regit_text": "등록 청구항 전문...",
        "claim_pub_text": "공개 청구항 전문..."
    },
    "components": [
        {"chunk_id": "...", "components": "구성요소 목록", "note": "종속항 참조"}
    ],
    "estoppel_claim_numbers": [],
    "source": "rag"
}
```

---

## 5단계: sLLM Generation (Qwen2.5-14B-FTO)

### 모델 정보

| 항목 | 값 |
|------|-----|
| 베이스 모델 | Qwen/Qwen2.5-14B-Instruct |
| 파인튜닝 | LoRA → 베이스 병합 완료 |
| 학습 데이터 | 17,377건 FTO 분석 데이터 |
| 서빙 | vLLM (OpenAI-compatible API) |
| GPU | A100 80GB |

### 분석 모드

#### 모드 A: sLLM 개별 분석 (generate)
```
검색 결과 Top 3건 → 특허 1건씩 sLLM 호출 (3회)
    │
    각 호출마다:
    ├── System Prompt (학습 데이터와 동일한 형식)
    └── User Prompt:
        ├── 사용자 제품 설명
        ├── [구성요소] (claim_components에서 추출)
        ├── [등록 청구항] 원문
        ├── [공개 청구항] 원문
        └── [특허 정보] 출원번호, 등록번호
    │
    ▼
  특허별 개별 분석 결과
```

#### 모드 B: FTO 통합 분석 (generate_fto, 현재 활성)
```
검색 결과 Top 5건 → 한번에 LLM 전달 → 3건 선별 + 종합 의견
    │
    ├── System Prompt: FTO 전문가 역할 + 분석 규칙
    └── User Prompt:
        ├── [사용자 제품] 제품 설명
        └── [특허 1~5] 각각:
            ├── 출원번호, 등록번호, 발명 명칭, 초록
            ├── 금반언 청구항 (삭제된 청구항 표시)
            ├── [구성요소] 사전 추출된 구성요소
            ├── [등록 청구항] (3,000자 제한)
            └── [공개 청구항] (3,000자 제한)
    │
    ▼
  LLM 출력 (3건 선별 분석 + 종합 FTO 의견)
```

### 시스템 프롬프트 핵심 규칙

| 규칙 | 설명 |
|------|------|
| 구성요소 완비의 원칙 | 특허의 **모든** 구성요소를 포함해야 침해 |
| 균등론 | 수치가 경미하게 이탈 시 "미대응(균등)" → 전문가 검토 |
| 금반언 | 공개→등록 시 삭제된 구성은 침해 주장 불가 |
| 내재성 | 성분 동일 + 용도/효과 미언급 시 "미대응(내재성)" |

### LLM 출력 형식

```markdown
## 특허 1: [등록번호]
◆구성 대비◆
| 특허 구성요소 | 제품 구성요소 | 대응 여부 |
|---|---|---|
| 헤스페리딘 0.1~5% | 헤스페리딘 2% | 대응 |
| 미백 효능 시험 | - | 미대응 |

◆판단◆
사용자 제품은 헤스페리딘을 포함하나...

◆결론◆
침해 가능성이 낮은 것으로 분석됩니다.

(특허 2, 3도 동일 형식)

◆종합 FTO 의견◆
제품 출시 관점에서 전체적인 침해 가능성 요약...
```

### 결론 라벨 분류
| 라벨 | 기준 |
|------|------|
| 침해 | "가능성이 높" 키워드 포함 |
| 비침해 | "가능성이 낮" 키워드 포함 |
| 침해_전문가 | "전문가" 키워드 포함 → 전문가 추가 검토 권고 |
| 애매 | "구체적인 실시 정보" 또는 "추가 정보" 포함 |

### 법리 일관성 검증 (자동)

LLM 출력 후 구성대비표와 결론의 논리적 정합성을 자동 검증:

| 규칙 | 불일치 조건 | 올바른 결론 |
|------|-----------|-----------|
| R9 | 침해 + 일반 미대응 존재 | → 비침해여야 함 |
| R10 | 비침해 + 전부 대응 | → 침해여야 함 |
| R11 | 전문가 + 균등/내재성 없음 | → 근거 없음 |
| R12 | 침해 + 균등/내재성 존재 | → 전문가여야 함 |
| R13 | 전문가 + 일반 미대응 존재 | → 비침해여야 함 |
| R14 | 비침해 + 균등/내재성만 | → 전문가여야 함 |

---

## 데이터 저장소 구조

```
┌──────────────────────────────────────────────────────┐
│                    데이터 저장소                        │
├──────────────┬──────────────┬────────────────────────┤
│  ChromaDB    │  BM25 Index  │  SQLite / MySQL RDS    │
│  (벡터 DB)   │  (역색인)     │  (관계형 DB)            │
├──────────────┼──────────────┼────────────────────────┤
│ KURE-v1      │ postings.pkl │ claim_keywords         │
│ 1024차원     │ idf.pkl      │  (1,000만건)            │
│ cosine 거리  │ doc_len.pkl  │ claim_components       │
│ ~78K 청크    │ doc_map.pkl  │  (26만건)               │
│              │ meta.json    │ parents.sqlite         │
│              │              │  (78,520건)             │
│              │              │ patents (RDS, 4만건)    │
└──────────────┴──────────────┴────────────────────────┘
```

---

## 검색 파라미터 요약

| 파라미터 | 값 | 설명 |
|---------|-----|------|
| PREFILTER_MAX_CHUNKS | 1,000 | 사전필터링 후 리트리버에 전달할 최대 청크 수 |
| DENSE_TOP_K | 50 | Dense 검색 반환 수 |
| BM25_TOP_K | 50 | Sparse 검색 반환 수 |
| RRF_K | 60 | RRF 순위 편향 완화 파라미터 |
| RRF_WEIGHTS | (0.2, 0.8) | (Dense, Sparse) 가중치 |
| FINAL_TOP_K | 10 | Patent Collapse 후 최종 특허 수 |
| MIN_SCORE | 0.010 | RRF 최소 점수 (노이즈 제거) |
| GENERATE_INPUT_N | 5 | LLM에 전달할 특허 수 |
| GENERATE_OUTPUT_N | 3 | LLM이 선별할 특허 수 |
| GENERATE_MAX_TOKENS | 4,096 | LLM 최대 출력 토큰 |
| GENERATE_TEMPERATURE | 0 | LLM 온도 (결정적 출력) |
| VLLM_TIMEOUT | 180초 | vLLM 호출 타임아웃 |
